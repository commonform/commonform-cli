Common Form command line interface

Usage:
  commonform read REFERENCE [options]
  commonform -h | --help
  commonform --version

Options:
  --color                     Force color terminal output
  -f FORMAT, --format FORMAT  Output format [default: terminal]
  -l LIB, --library LIB       The library to use
